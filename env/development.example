# ── Development only (`npm run dev`) ──
# Next.js loads this as `.env.development.local` (not used by production builds).
#
#   cp env/development.example .env.development.local

# UI preview without qBittorrent (safe for localhost).
DEV_PREVIEW=1
NEXT_PUBLIC_DEV_PREVIEW=1

# Optional — fill when linking real qB locally (and set both PREVIEW flags to 0):
# QBITTORRENT_URL=https://your-qb.example.com
# QBITTORRENT_USERNAME=
# QBITTORRENT_PASSWORD=
#
# Web App（Google OAuth）：
# GOOGLE_CLIENT_ID=
# ALLOWED_GOOGLE_EMAILS=you@gmail.com
